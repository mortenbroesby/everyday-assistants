OpenSpec skills are thin adapters: inspect current CLI state before acting.
For registered standalone stores, resolve the store once with
`openspec store list --json` and retain `--store <id>` on supported commands.
The GitHub issue remains the work contract; OpenSpec owns only durable
specification. Never invent workflow state from static prose.
