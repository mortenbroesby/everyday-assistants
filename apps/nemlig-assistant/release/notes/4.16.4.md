# Nemlig Assistant 4.16.4 - Reopen

## In plain language

Returning to a shopping conversation shows an inactive card. Open the current
review to continue with today’s draft instead of an outdated basket snapshot.

## Changes

- Recover conflicting review revisions with one read-only refresh and no replay.
- Hide editing controls after connection failures and replace raw protocol errors with clear recovery guidance.
- Keep retired viewer addresses available as inactive notices instead of broken templates.
- Preserve the local Basket → exact review → explicit approval → Nemlig transfer flow.
- Add executable recovery regressions and a real-MCP browser smoke for remounts, conflicts, outages and restarts.

ChatGPT can retain already cached old documents; refresh app metadata and open
the current review to use the v4 interface.
